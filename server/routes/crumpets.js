const express = require('express');
const router = express.Router();
const { Crumpet } = require('../db');

/*
requirements:
- get route "/" --> '/api/crumpets'
- get route "/:id"
- post route "/"
- put route "/:id"
- delete route "/:id"
*/

module.exports = router;
