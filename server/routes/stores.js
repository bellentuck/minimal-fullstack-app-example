const express = require('express');
const router = express.Router();
const { Store } = require('../db');

/*
requirements:
- get route "/"
- get route "/:id"
- post route "/"
- put route "/:id"
- delete route "/:id"
*/

module.exports = router;
