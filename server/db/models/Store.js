const Sequelize = require('sequelize');

const db = require('../_db');

/*
example store from seed file:
{
  name: 'OG Crumpets',
  location: 'NYC',
  status: 'Open'
}
*/

const Store = null;

module.exports = Store;
